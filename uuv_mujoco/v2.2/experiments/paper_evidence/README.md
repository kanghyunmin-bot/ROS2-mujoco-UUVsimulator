# Paper Evidence

This folder is for artifacts that can support thesis/paper writing.

Required metadata for each artifact set:

- exact input source,
- exact command,
- figure/table file paths,
- metric definitions,
- known exclusions and limitations,
- contract version used.

Do not place a figure here unless its generation path can be repeated.
