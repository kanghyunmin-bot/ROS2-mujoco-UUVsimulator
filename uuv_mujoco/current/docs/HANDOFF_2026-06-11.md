# UUV MuJoCo v2.2 Handoff - 2026-06-11

## Current Objective

Make `uuv_mujoco/v2.2` maintainable enough to continue safely, while preserving
the controller/plant contract that ultimately matters:

```text
real RCOU/PWM history
  -> MuJoCo plant replay
  -> simulated Bar30/IMU/DVL/local-pose outputs
  -> compare against real rosbag sensors
```

The final target is still not "make the vehicle move roughly".  The target is
that real actuator history produces sensor/state trends close enough to the real
vehicle that remaining errors can be attributed to calibrated dynamics, not to
broken input/output contracts.

## Hard Boundaries

- Do not modify `/Users/kanghyunmin/Desktop/uuv_sim/ardupilot`.
- Do not change the ArduPilot submodule pointer.
- Keep edits inside `/Users/kanghyunmin/Desktop/uuv_sim/uuv_mujoco/v2.2` unless
  the user explicitly widens scope.
- Do not hide mismatch with ALT_HOLD shims, PWM remaps, artificial output offsets,
  or JSON-servo downsampling.
- Controller parity surface is telemetry-to-telemetry:
  `real /mavros/rc/out` vs SITL MAVLink `SERVO_OUTPUT_RAW`.
- Plant input surface is raw ArduSub JSON servo or real RCOU plant replay,
  depending on mode.  Do not mix these observation points.

## Current Green Gate

Fast static/non-live sanity now has a single runner:

```bash
cd /Users/kanghyunmin/Desktop/uuv_sim
env PYTHONPYCACHEPREFIX=/private/tmp/pycache \
  python3 uuv_mujoco/v2.2/tools/run_fast_contract_sanity.py \
  --include-host \
  --out-dir uuv_mujoco/v2.2/research_workspace/00_current_contract/fast_contract_sanity_host_20260611
```

Latest result:

```text
PASS compileall_core
PASS rc_frame_contract
PASS ros2_command_payload
PASS sitl_command_link_readiness
PASS runtime_readiness_policy
PASS gui_backend_selection
PASS gui_readiness_contract
PASS gui_initial_depth_contract
PASS initial_hold_pose
PASS real_start_measurements
PASS physics_contract_geometry
PASS physics_runtime_hydrostatic
PASS thruster_param_loader
PASS odometry_publish_builders
PASS ros2_sitl_command_override
PASS source_contract_audit
PASS dev_os_compat_headless
```

Artifacts:

- `research_workspace/00_current_contract/fast_contract_sanity_20260611/`
- `research_workspace/00_current_contract/fast_contract_sanity_host_20260611/`

## What Was Cleaned Up Most Recently

- `tools/run_fast_contract_sanity.py`
  - New batch sanity runner for handoff and refactor safety.
- `tools/axis_rc_sampling.py`
  - Kept as the public sample builder.
- `tools/axis_rc_sample_channels.py`
  - RC channel sample fields.
- `tools/axis_rc_sample_motion.py`
  - IMU, DVL, odometry, and depth sample fields.
- `tools/audit_code_contract_runtime_identity_inputs.py`
  - Dataclass and public input assembly only.
- `tools/audit_code_contract_runtime_identity_sources.py`
  - Runtime alias/version/git source probes.
- `tools/audit_code_contract_thruster_conversion.py`
  - Public check builder only.
- `tools/audit_code_contract_thruster_conversion_rules.py`
  - Boolean source-token contract rules.
- `tools/audit_code_contract_thruster_conversion_evidence.py`
  - Evidence row construction.

## Preserve Intentionally Complex Runtime Code

These files score high in the refactor inventory, but the complexity is contract
or physics complexity.  Do not split them just because they score high:

- `sim/physics/fossen_residual_runtime_builder.py`
- `bridge/sitl_math.py`
- `sim/runtime/sitl_servo_binding.py`
- `bridge/ros2_state_sitl_velocity.py`
- `sim/transport/mavlink_message_interval_send.py`
- `sim/contracts/rc_value_rules.py`
- `bridge/sitl_rc_override_keepalive.py`
- `sim/physics/dynamic_fluidcoef_pattern_prepare.py`
- `sim/runtime/underwater_hydrostatic_runtime.py`
- `bridge/sitl_transport_model_vertical.py`
- `bridge/sitl_rc_override_send.py`
- `bridge/sitl_external_nav_vpd_pose.py`

Only touch those with a focused failing check or a live trace that identifies the
bug.

## Next Work That Actually Moves The Goal

1. Keep the fast sanity runner green after any cleanup.
2. Run a live GUI/MuJoCo/SITL smoke:
   - GUI starts.
   - MuJoCo start reaches ready.
   - arm/mode command is acknowledged.
   - MANUAL RC override moves the vehicle with no multi-second delay.
   - RCOU/servo telemetry is logged.
3. Run real RCOU plant replay against the 90s bag baseline.
4. Compare simulated sensors against real sensors:
   - depth/baro depth
   - roll/pitch/yaw
   - gyro axes
   - DVL/body velocity
   - local pose
   - use correlation/phase first, RMSE second.
5. Only after input/output timing and frame contracts are proven should dynamics
   coefficients be tuned.

## Current Risk

The static contract gate is green.  That does not prove the live GUI/SITL path is
fully good.  The remaining risk is runtime integration:

- GUI start button process orchestration.
- MuJoCo ready vs actual command acceptance.
- RC override latency in the viewer.
- ArduSub arm/mode path under the selected command link.
- Real RCOU plant replay time alignment.
- Sensor frame/rate/phase alignment.

## Progress Estimate

- Refactor/maintainability: about 65%.
- Static contract coverage: about 75%.
- Live GUI/SITL confidence: about 50%.
- Plant replay sensor parity: about 40%.
- Overall project target: about 64%.
