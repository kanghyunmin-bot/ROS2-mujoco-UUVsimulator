# Controller Debug Analysis

This directory contains controller and plant-replay research that was removed
from the active simulator runtime.

Files here are optional analysis utilities. The simulator does not import this
directory during a normal run.
