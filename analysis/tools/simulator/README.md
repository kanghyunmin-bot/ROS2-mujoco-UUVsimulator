# Simulator Analysis Tools

Offline diagnostics, controller-parity analysis, plotting, parameter sweeps,
and research asset generation live here.

The active simulator runtime and its preflight/contract checks remain under
`sim/current/tools`. Run scripts in this directory from the workspace root so
their runtime paths resolve to `sim/current`.
