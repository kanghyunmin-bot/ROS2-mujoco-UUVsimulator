# Optional VLA checkpoints

Dataset collection does not require a model. Install a complete CAP-free checkpoint
with `./release.sh install-model /path/to/checkpoint`. Model shards are release
assets, not source-code Git files. See `docs/RELEASE_INSTALL.md`.
